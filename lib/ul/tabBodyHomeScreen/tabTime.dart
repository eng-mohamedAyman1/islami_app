import 'package:flutter/material.dart';

class Tabtime extends StatelessWidget {
  const Tabtime({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
